# サポート / Support

## SELECT for macOS サポートページ

SELECT をご利用いただきありがとうございます。

## お問い合わせ

アプリに関するご質問、不具合のご報告、ご要望などがございましたら、下記メールアドレスまでお気軽にお問い合わせください。

**メール:** russell2026@outlook.jp

件名に「SELECT for macOS フィードバック」と記載いただけるとスムーズに対応できます。

## よくある質問

**Q. メニューバーアイコンが多すぎる / 邪魔**
A. タブごとの表示有無は「設定 → メニューバー」の per-tab トグルで個別に切替えできます。SELECT 本体のアイコン (♻) は常に残るので、不要なタブを OFF にすると見た目がすっきりします。

**Q. メニューバーアイコンをシンプルにしたい**
A. 「設定 → メニューバー → ラベル表示」で「シンプルアイコン」を選択してください。スマホ / 脳のアイコンが抜けて、矢印 + 丸枠だけのミニマル表示になります。**モード単位で設定可能**で、モードを切替えると別のスタイルに自動で切り替わります。

**Q. フローティングバーを表示 / 非表示にしたい**
A. デフォルトのグローバルホットキー **`cmd + shift + 0`** で toggle できます。または「設定 → フローティングバー → 起動時に表示」を OFF にすると、起動時から非表示状態になります。表示中はメニューバーの ♻ アイコンをクリックして開くドロップダウンからも切替できます。

**Q. フローティングバーが他のアプリの後ろに隠れる**
A. フローティングバー右端のピンボタン (📌) をクリックして常時最前面 ON にしてください。OFF だと通常ウィンドウ扱いになり、他アプリで作業すると後ろに回ります。

**Q. フローティングバーの位置を変えたい**
A. バー本体をドラッグすれば任意の位置に移動できます。移動完了後 0.2 秒で自動保存され、次回起動時にもその位置で復元されます。外部モニタを外して画面外になった場合は、自動でメイン画面の左上に戻ります。

**Q. グローバルホットキーを変更したい**
A. 「設定 → フローティングバー → ホットキー」のテキストフィールドで書き換えてください。形式は `<modifier>+...+<key>` (例: `cmd+ctrl+space`)。使えるキー: cmd / shift / ctrl / opt + 0-9 / a-z / space / return / tab / esc。確定すると即座に反映されます。ホットキーはモード共通 (app-global) です。

**Q. グローバルホットキーが効かない**
A. 他のアプリが同じキー組合せを先に登録している可能性があります。別の組合せに変更してみてください。デフォルトの `cmd+shift+0` は他アプリの `cmd+shift+s` (Save As 系) との衝突を避けるよう選んであります。

**Q. Finder / メール / ターミナル などの内蔵アプリが起動しない**
A. v1.x 以降の path 種別で起動します。アイテム編集画面で「アプリのパス」が正しく設定されているか確認してください (例: `/System/Library/CoreServices/Finder.app`)。

**Q. Dock アイコンを消したい**
A. 「設定 → 起動とウィンドウ → Dock にアイコンを表示」を OFF にしてください。OFF にすると Cmd+Tab スイッチャーにも出なくなり、**メニューバー + フローティングバーのみ常駐** という運用になります。

**Q. ログイン時に自動起動させたい**
A. 「設定 → 起動とウィンドウ → ログイン時に起動」を ON にしてください。macOS の Login Items に登録されます。

**Q. モード (プロフィール) を追加したい**
A. サイドバー上部の「**追加**」ボタンから作成できます。Mac 版では IAP gating はなく、無制限に作成可能です。**新規モード追加時は、その時点でアクティブだったモードの設定 (メニューバー / フローティングのスタイル、各タブの表示 ON/OFF) が自動コピー** されるので、ゼロから設定しなおす必要はありません。

**Q. モードの名前 / アイコンを変えたい**
A. サイドバー上部の「**編集**」ボタンから、名前と SF Symbol アイコンを変更できます。Built-in モード (= 最初のデフォルトモード) も編集可。削除はできません。

**Q. メニューバー / フローティングの設定がモードごとに違うのはなぜ?**
A. 仕様です。モードごとに「使うシチュエーション」が違うので、表示スタイル (シンプルアイコン / アイコン+テキスト) や、表示タブの組合せをモード単位で切替えられるようにしています。ホットキーとフローティング位置 / ピン状態はモード共通 (app-global) です。

**Q. iCloud 同期は必須?**
A. いいえ、iCloud にサインインしていない場合は端末ローカルのみで動作します。サインイン済みなら自動で同期されます。同期データはユーザーご自身の CloudKit Private Database に保存され、開発者を含む第三者はアクセスできません。

**Q. 別の Mac にデータを移行したい**
A. 推奨は **iCloud 同期** (同じ Apple ID でサインインしていれば自動)。手動で移行したい場合は「設定 → サポート → JSON にエクスポート」でファイル保存 → 別 Mac の「JSON からインポート」で取り込めます。

**Q. JSON インポートで既存データはどうなる?**
A. UUID で upsert する仕様なので、**同じ UUID のアイテム / モードは上書き、新しい UUID は追加** されます。既存データを完全に置き換えたい場合は、先にローカルデータを削除してから import してください。

**Q. アプリ検索でアプリが見つからない**
A. SELECT は Apple の iTunes Search API を使って App Store からアプリを検索します。「設定 → 言語と地域 → 国コード」が App Store の国 (例: 日本なら `jp`、米国なら `us`) と合っているか確認してください。

**Q. アイテムを削除したい**
A. アイテム一覧の行を右クリック → 削除、または編集モードに入り選択して一括削除できます。

**Q. アイテムを別タブ / 別モードに移動したい**
A. 編集モードで選択 → 「移動」または「コピー」を押すと、移動先 (別タブ or 別モード) を選択するシートが開きます。フォルダ内へのドラッグ＆ドロップにも対応しています。

**Q. データを完全に削除したい**
A. 以下の手順で完全削除できます:
- ローカル: **Finder → 移動 → ライブラリへ移動 → Containers → `com.artemis.select.mac` フォルダ** を削除
- iCloud: **システム設定 → Apple ID → iCloud → iCloud で同期するアプリを管理 → SELECT** から iCloud データを削除
- アプリ本体: **Finder → アプリケーション → SELECT.app をゴミ箱へ**

**Q. アプリの表示言語を変更したい**
A. 「設定 → 言語 → 日本語 / English」。アプリ再起動後に反映します。これは SELECT 単体の設定で、macOS のシステム言語とは独立です。

**Q. 設定画面の「ホットキー」入力欄が常に選択状態になる**
A. v1.x 以降で修正済み。設定画面を開き直して再現する場合はお問い合わせください。

---

制定日: 2026 年 5 月 30 日

English version: [Support (macOS)](./Select-SUPPORT-mac-en)
