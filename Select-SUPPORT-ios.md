# サポート / Support

## SELECT サポートページ

SELECT をご利用いただきありがとうございます。

## お問い合わせ

アプリに関するご質問、不具合のご報告、ご要望などがございましたら、下記メールアドレスまでお気軽にお問い合わせください。

**メール:** russell2026@outlook.jp

件名に「SELECT フィードバック」と記載いただけるとスムーズに対応できます。

## よくある質問

**Q. ウィジェットからアプリを開くとSELECTが一瞬表示される**
A. iOS のウィジェット拡張はカスタム URL スキームを直接呼び出せないため、初期状態では SELECT を一度経由して対象アプリが起動します。これを解消するには、Apple 純正の Shortcuts アプリに「SELECT Widgets」ショートカットを **1 回だけ** 登録してください。アプリ内の「設定 → ウィジェット起動高速化」または初回オンボーディングの「起動を高速化」画面から導入できます。

**Q. ウィジェットをタップしても何も起こらない**
A. 以下を順にご確認ください:
- 「SELECT Widgets」ショートカットが Shortcuts アプリに登録されているか (設定 → ウィジェット起動高速化)
- 対象アプリが端末にインストールされているか
- ホーム画面に置いたウィジェットを長押し→「ウィジェットを編集」で正しいモードが選択されているか

**Q. アプリ追加で対象アプリが検索できない**
A. SELECT は Apple の iTunes Search API を使って App Store からアプリを検索します。検索国を変更してみてください (設定 → アプリ検索 → 検索する国・地域)。それでも見つからない場合は、対象アプリが該当国の App Store に登録されていない可能性があります。

**Q. アプリは検索結果に出るが、URL Scheme が見つからないと表示される (旧版)**
A. v1.x 以降は URL Scheme が不要になりました。SELECT Widgets ショートカット経由で bundle ID から直接起動するため、どのアプリも追加・起動できます。

**Q. Pro 機能の解約方法**
A. SELECT Pro は **買い切り型** であり、サブスクリプションではありません。一度購入すると永続的に利用できます。誤って購入された場合は App Store のサポートから返金手続きをご検討ください。

**Q. Pro 機能を別の端末で復元したい**
A. アプリ内の「設定 → Pro → 購入を復元」をタップしてください。同じ Apple ID でサインインしている端末で復元できます。

**Q. iCloud 同期について教えてほしい**
A. Pro 版を購入すると、登録したアプリやモード設定が iCloud 経由で複数の Apple デバイス間で同期されます。同期は iOS の「設定 → [ユーザー名] → iCloud → SELECT」から有効/無効を切替えられます。

**Q. ウィジェットに表示できるアプリ数の制限**
A. iOS ウィジェットの仕様により、1 画面に表示できるアプリ数は端末サイズに依存します (Large ウィジェットで約 5〜7 アプリ)。それ以上のアプリを登録した場合、ウィジェット上部の「→」ボタンでページ送りできます。フォルダ機能 (Pro 限定) で階層化することもできます。

**Q. モード (プロフィール) の追加方法**
A. アプリ上部のモード名 (例: 「外出」) の隣にあるドロップダウンをタップし、シート下部の「モードを追加」から作成できます。**複数モード作成は Pro 限定** 機能です。

**Q. アイテムの削除方法**
A. 3 通りあります:
- **スワイプ**: 行を右から左にスワイプして「削除」をタップ
- **選択モード**: 上部の「選択」ボタンから複数選択して「削除」
- **タップ**: 行をタップして編集画面から削除 (個別)

削除後は元に戻せません。

**Q. アイテムを別のタブやモードに移動・コピーしたい**
A. 「選択」ボタンで選択モードに入り、対象アイテムを選択した状態で「移動」または「コピー」を押すと、移動先 (別タブ・別モード) を選択するシートが開きます。

**Q. ウィジェットの色を固定したい**
A. 「設定 → ウィジェットの色」でホワイト / ダーク / 端末設定に合わせる の 3 種類から選択できます。すべてのモードに共通で適用されます。

**Q. iOS 18 の「色合い調整」表示でウィジェットの色が変**
A. iOS 18 のホーム画面「テイント」表示は Apple の仕様で、ウィジェットの色が自動でテイント変換されます。SELECT はアプリアイコンに `widgetAccentedRenderingMode(.fullColor)` を適用して可能な限り元の色を保つよう実装していますが、背景色など完全には防げない箇所があります (Apple 仕様)。

**Q. ウィジェットのタブ表示をアイコンとテキストで切替えたい**
A. 「設定 → タブの表示」で「Icon」または「Text」を選択できます。アプリ内とウィジェットの両方に反映されます。

**Q. アプリアイコンを表示せずテキストだけで使いたい**
A. モード切替シート (上部のモード名タップ) で、各モードに「アプリアイコンを表示」トグルがあります。OFF にすると当該モードはテキストのみのリスト表示になります。

**Q. Enjoy タブを非表示にしたい**
A. モード切替シートで各モードに「Enjoy タブを非表示」トグルがあります。ON にすると 4 タブ表示になり、Enjoy に登録済みのアイテムは保持されたまま見えなくなります。

**Q. データを完全に削除したい**
A. アプリのアンインストール (iOS の設定 → 一般 → iPhone ストレージ → SELECT → App を削除) で端末内のデータが全削除されます。iCloud 同期を有効にしていた場合は、追加で「設定 → [ユーザー名] → iCloud → ストレージを管理 → SELECT → データを削除」で iCloud 側も削除できます。

**Q. アプリの表示言語を変更したい**
A. iOS の「設定 → 一般 → 言語と地域」でデバイス全体の言語、または「設定 → SELECT → 言語」で本アプリ単体の言語を切替えられます。

---

制定日: 2026 年 5 月 19 日

English version: [Support (iOS)](./Select-SUPPORT-ios-en)
